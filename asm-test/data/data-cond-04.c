#include <stdio.h>

int main()
{
        long timeout = 0;

        //blez, j
        if (timeout > 0) {
                timeout = 1;
        } else {
                timeout = 0;
        }

        printf("%ld", timeout);

        return 0;
}
